# INSTALLATION INSTRUCTIONS

```
sudo apt-get -y install build-essential pkg-config libgsl-dev libblas3gf libblas-dev liblapack3gf liblapack-dev graphviz openscenegraph libopenscenegraph-dev gnuplot libgts-bin autoconf libtool libcln6 libcln-dev ginac-tools libginac-dev
```

```
cd lib_3d_mec_ginac
make clean

./autogen.sh --prefix=`pwd`/..
make
make install
```
